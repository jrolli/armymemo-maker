/**
 * The armymemo package version this bundle ships. Single source for the
 * version pin the Markdown converter emits in its `#import` line; must match
 * the vendored tarball, the acknowledgements inventory, and the starter
 * example's `#import` — scripts/check-acknowledgements.mjs fails the build
 * when any of them disagree (design D6 of add-markdown-input).
 */
export const ARMYMEMO_VERSION = "0.2.3";
