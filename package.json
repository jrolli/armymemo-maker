{
  "name": "memo-army-dev",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && npm run check:local-only",
    "check:local-only": "node scripts/check-local-only.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@myriaddreamin/typst-ts-web-compiler": "0.7.0",
    "@myriaddreamin/typst.ts": "0.7.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
